import styled from 'styled-components';

const Wrapper = styled.header`
  background: #363c3c;
  font-size: 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 100px;
  padding: 0 30px;
`;

export const S = {
  Wrapper,
};

# Migration `20200803041414-create_initial_schema`

This migration has been generated by siongesteban at 8/3/2020, 4:14:14 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "swatch_palette_type" AS ENUM ('SHADE', 'TINT');

CREATE TYPE "swatch_color_number" AS ENUM ('N100', 'N200', 'N300', 'N400', 'N500');

CREATE TABLE "public"."color" (
"id" SERIAL,
"name" text  NOT NULL ,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."swatch" (
"id" SERIAL,
"hex" text  NOT NULL ,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"parent_color_id" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."swatch_color" (
"id" SERIAL,
"hex" text  NOT NULL ,
"number" "swatch_color_number" NOT NULL ,
"palette_type" "swatch_palette_type" NOT NULL ,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"swatch_id" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "unique_color" ON "public"."color"("name")

CREATE UNIQUE INDEX "unique_swatch" ON "public"."swatch"("hex","parent_color_id")

CREATE UNIQUE INDEX "unique_swatch_color" ON "public"."swatch_color"("hex","swatch_id")

ALTER TABLE "public"."swatch" ADD FOREIGN KEY ("parent_color_id")REFERENCES "public"."color"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."swatch_color" ADD FOREIGN KEY ("swatch_id")REFERENCES "public"."swatch"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200803041414-create_initial_schema
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,67 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Color {
+  id        Int      @default(autoincrement()) @id
+  name      String
+  createdAt DateTime @map("created_at") @default(now())
+
+  swatches Swatch[]
+
+  @@map("color")
+  @@unique([name], name: "unique_color")
+}
+
+model Swatch {
+  id        Int      @default(autoincrement()) @id
+  hex       String
+  createdAt DateTime @map("created_at") @default(now())
+
+  parentColor   Color @relation(fields: [parentColorId], references: [id])
+  parentColorId Int   @map("parent_color_id")
+
+  swatchColors SwatchColor[]
+
+  @@map("swatch")
+  @@unique([hex, parentColorId], name: "unique_swatch")
+}
+
+model SwatchColor {
+  id          Int               @default(autoincrement()) @id
+  hex         String
+  number      SwatchColorNumber
+  paletteType SwatchPaletteType @map("palette_type")
+  createdAt   DateTime          @map("created_at") @default(now())
+
+  swatchId Int    @map("swatch_id")
+  swatch   Swatch @relation(fields: [swatchId], references: [id])
+
+  @@map("swatch_color")
+  @@unique([hex, swatchId], name: "unique_swatch_color")
+}
+
+enum SwatchPaletteType {
+  SHADE
+  TINT
+
+  @@map("swatch_palette_type")
+}
+
+enum SwatchColorNumber {
+  N100
+  N200
+  N300
+  N400
+  N500
+
+  @@map("swatch_color_number")
+}
```



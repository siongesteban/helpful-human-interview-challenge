# Migration `20200731185500-create_initial_shade_table`

This migration has been generated by siongesteban at 7/31/2020, 6:55:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "shade_number" AS ENUM ('S100', 'S200', 'S300', 'S400', 'S500', 'S600', 'S700');

CREATE TABLE "public"."color" (
"id" SERIAL,
"name" text  NOT NULL ,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."shade" (
"id" SERIAL,
"hex" text  NOT NULL ,
"number" "shade_number" NOT NULL ,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"color_id" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "unique_color" ON "public"."color"("name")

CREATE UNIQUE INDEX "unique_shade" ON "public"."shade"("number","color_id")

ALTER TABLE "public"."shade" ADD FOREIGN KEY ("color_id")REFERENCES "public"."color"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200731182349-make_color_name_unique..20200731185500-create_initial_shade_table
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,9 +12,35 @@
 model Color {
   id        Int      @default(autoincrement()) @id
   name      String
-  createdAt DateTime @default(now()) @map("created_at")
+  shades    Shade[]
+  createdAt DateTime @map("created_at") @default(now())
-  @@map(name: "color")
+  @@map("color")
   @@unique([name], name: "unique_color")
 }
+
+model Shade {
+  id        Int         @default(autoincrement()) @id
+  hex       String
+  number    ShadeNumber
+  createdAt DateTime    @map("created_at") @default(now())
+
+  color   Color @relation(fields: [colorId], references: [id])
+  colorId Int   @map("color_id")
+
+  @@map("shade")
+  @@unique([number, colorId], name: "unique_shade")
+}
+
+enum ShadeNumber {
+  S100
+  S200
+  S300
+  S400
+  S500
+  S600
+  S700
+
+  @@map("shade_number")
+}
```


